# If set, the app will try to use the REST backend. If missing/unreachable, it falls back to localStorage.
# Example: http://localhost:8000
REACT_APP_API_BASE=

# Alternate supported name (either can be used)
REACT_APP_BACKEND_URL=
